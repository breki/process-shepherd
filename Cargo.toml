[package]
name = "process-shepherd"
version = "0.1.0"
edition = "2021"
authors = ["breki"]
description = "A CLI utility to track CPU usage per process on Windows"

[dependencies]
sysinfo = "0.32"
chrono = "0.4"
